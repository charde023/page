---
title: 3주차 실습세션 — 에이전트 활용과 마무리 실습
eyebrow: 지피터스 22기 · 끌림 영상 스터디
subtitle: 2026-06-02 라이브 강의 정리본
source: 2026-06-02 [지피터스] 22기 3주차 실습세션.mp4
description: Google Workspace CLI 설치, OAuth 인증, GWS 명령 구조, AI 에이전트 스킬 연동까지 따라 하는 3주차 실습 정리.
date: 2026-06-02
---

# 3주차 실습세션 — 에이전트 활용과 마무리 실습

> 지피터스 22기 · 2026-06-02 / 원본: `2026-06-02 [지피터스] 22기 3주차 실습세션.mp4` · 약 88분

## 결론 (TL;DR)

> Google Workspace CLI를 설치하고 OAuth 인증을 붙인 뒤, GWS 명령을 AI 에이전트 스킬로 연결하면 자연어 요청으로 캘린더·드라이브·메일 같은 워크스페이스 작업을 실행할 수 있다.

| 구분 | 핵심 메시지 | 실습에서 확인한 장면 |
|---|---|---|
| 왜 필요한가 | 브라우저 클릭 대신 터미널 명령으로 Google Workspace 데이터를 다룬다 | 캘린더 일정, 드라이브, 지메일 정보를 CLI로 조회 |
| 무엇을 설치하나 | `gws`와 Google Cloud CLI가 모두 필요하다 | `gws --version`, `gws services`로 설치 확인 |
| 어디서 막히나 | OAuth 동의 화면, 클라이언트 ID·시크릿, 권한 범위 설정에서 많이 막힌다 | Google Cloud Console에서 OAuth 클라이언트 생성 |
| 어떻게 에이전트화하나 | GWS 사용법을 에이전트 스킬로 만들어 자연어 요청에 연결한다 | Claude Code에 GWS 스킬 생성을 요청하고 재시작 후 사용 |

| # | 챙겨야 할 것 | 한 줄 |
|---:|---|---|
| 1 | 먼저 실행 환경을 점검한다 | Node, npm, GWS, Google Cloud CLI가 준비되어야 한다 |
| 2 | GWS 설치를 끝낸다 | npm, Homebrew 등 환경에 맞는 설치 방식을 고른다 |
| 3 | Google Cloud 프로젝트를 잡는다 | 새 프로젝트가 안 되면 기존 프로젝트를 선택해도 된다 |
| 4 | OAuth 클라이언트를 만든다 | 애플리케이션 유형은 데스크톱 앱으로 둔다 |
| 5 | 접근 범위는 좁게 시작한다 | 처음에는 추천 읽기 권한으로 충분하다 |
| 6 | CLI 단독 동작을 먼저 검증한다 | `gws calendar agenda`처럼 직접 조회가 되는지 본다 |
| 7 | 그 다음 에이전트에 연결한다 | 스킬 설치 후 에이전트를 재시작해야 인식된다 |

| 실행 순서 | 목적 | 성공 기준 | 막히면 확인할 것 |
|---:|---|---|---|
| 1 | 환경 점검 | 필요한 CLI 목록이 확인된다 | 터미널 경로, Node/npm 설치 여부 |
| 2 | GWS 설치 | `gws --version`이 버전을 출력한다 | npm 또는 Homebrew 설치 로그 |
| 3 | 서비스 목록 확인 | `gws services`로 지원 서비스가 보인다 | GWS 명령이 PATH에 잡혔는지 |
| 4 | Google Cloud CLI 설치 | `gcloud` 인증 단계로 넘어간다 | Google Cloud CLI 미설치 메시지 |
| 5 | `gws auth setup` 실행 | 계정 로그인과 프로젝트 선택 화면이 나온다 | 로그인 계정, 프로젝트 권한 |
| 6 | OAuth 클라이언트 생성 | 클라이언트 ID와 보안 비밀번호를 얻는다 | OAuth 동의 화면, 데스크톱 앱 유형 |
| 7 | 권한 범위 선택 | 인증 URL 승인 후 success가 나온다 | 권한 미선택, 확인되지 않은 앱 경고 |
| 8 | GWS 명령 테스트 | 캘린더 일정 등 데이터가 출력된다 | API 활성화, OAuth 범위 |
| 9 | 에이전트 스킬 생성 | GWS 관련 스킬 파일이 만들어진다 | 프로젝트 범위 설치 여부 |
| 10 | 에이전트 재시작 | 자연어로 일정 조회가 동작한다 | 재시작 누락, 스킬 인식 실패 |

## 강의는 어떤 내용이었나

이번 실습은 Google Workspace CLI, 줄여서 GWS를 설치하고 인증한 뒤, AI 에이전트가 그 CLI를 호출할 수 있게 만드는 흐름을 다뤘다. 앞부분에서는 왜 브라우저 클릭을 CLI 명령으로 바꾸는지 설명했고, 중반부터는 실제 터미널과 Google Cloud Console을 오가며 설치·OAuth·권한 설정을 진행했다.

후반부에서는 GWS가 단독으로 캘린더 정보를 불러오는지 확인한 다음, Claude Code 같은 에이전트에 GWS 스킬을 만들게 했다. 핵심은 API 코드를 직접 작성하는 것이 아니라, 이미 준비된 CLI와 스킬 문서를 에이전트가 읽고 자연어 요청을 실행 가능한 명령으로 바꾸게 하는 것이다.

| 파트 | 내용 | 실습 난이도 | 핵심 산출물 |
|---|---|---|---|
| 개념 소개 | Workspace 제어가 브라우저 클릭에서 CLI와 에이전트 호출로 이동하는 흐름 | 낮음 | GWS의 역할 이해 |
| 설치 | GWS와 Google Cloud CLI 설치 및 버전 확인 | 중간 | 로컬 터미널에서 `gws` 실행 가능 |
| 인증 | Google Cloud 프로젝트, OAuth 동의 화면, 클라이언트 ID·시크릿 설정 | 높음 | GWS OAuth 인증 완료 |
| 명령 구조 | 서비스명, 액션, 플래그로 구성되는 GWS 명령 해부 | 중간 | 직접 명령으로 Workspace 데이터 조회 |
| 에이전트 연동 | Claude Code에 GWS 스킬 생성 요청, 재시작 후 자연어 호출 | 중간 | 자연어 기반 일정 조회 |

## 0. 한눈에 보는 GWS 에이전트 실습 흐름

1. Google Workspace CLI가 어떤 문제를 푸는지 이해한다.
2. 실습 폴더와 공유 자료를 열어 빠른 시작 순서를 확인한다.
3. 로컬 환경에 Node, npm, GWS가 있는지 점검한다.
4. GWS를 설치하고 버전과 지원 서비스 목록을 확인한다.
5. Google Cloud CLI를 설치하고 계정 로그인을 진행한다.
6. Google Cloud 프로젝트를 선택하거나 새로 만든다.
7. 필요한 Workspace API를 활성화한다.
8. OAuth 동의 화면과 데스크톱 앱 클라이언트를 만든다.
9. 클라이언트 ID와 시크릿을 `gws auth setup`에 입력한다.
10. 권한 범위를 고르고 인증 URL에서 승인한다.
11. `gws calendar agenda` 같은 명령으로 단독 동작을 확인한다.
12. AI 에이전트에 GWS 스킬 생성을 요청한다.
13. 에이전트를 재시작한 뒤 자연어로 일정 조회를 실행한다.

## 1. Workspace 제어는 왜 CLI와 에이전트로 넘어가는가

기존에는 구글 캘린더, 드라이브, 지메일을 쓰려면 웹 브라우저에서 직접 클릭하고 입력했다. 내부적으로는 API 호출이 일어나지만, 사용자는 API를 직접 다루지 않고 화면을 조작한다. GWS는 이 과정을 터미널 명령으로 감싼다.

강의에서 강조한 변화는 다음과 같다. 사용자가 직접 Google API를 호출하는 코드를 만들지 않아도, `gws calendar ...`, `gws drive ...`, `gws gmail ...` 같은 명령으로 구조화된 JSON 결과를 받을 수 있다. 여기에서 한 단계 더 가면 AI 에이전트가 사용자의 자연어 요청을 읽고, 적절한 GWS 명령과 인자를 구성한다.

| 방식 | 사용자가 하는 일 | 시스템이 하는 일 | 장점 | 한계 |
|---|---|---|---|---|
| 브라우저 클릭 | 화면을 열고 버튼을 누른다 | 웹 앱이 API를 호출한다 | 익숙하다 | 반복 작업 자동화가 어렵다 |
| 직접 API 호출 | API 문서를 보고 코드를 작성한다 | 코드가 API를 호출한다 | 세밀한 제어가 가능하다 | 설정·권한·코드 부담이 크다 |
| GWS CLI | 터미널 명령을 실행한다 | CLI가 Workspace API를 감싼다 | 빠르고 구조화된 결과를 얻는다 | 명령 구조를 알아야 한다 |
| AI 에이전트 + GWS | 자연어로 요청한다 | 에이전트가 GWS 명령을 구성한다 | 업무 요청에 가깝게 실행된다 | 초기 스킬과 인증 설정이 필요하다 |

## 2. GWS와 Google Cloud CLI 설치

실습은 먼저 환경 점검에서 시작했다. 강사는 실습 자료에 빠른 시작 명령을 준비해 두었고, 참가자들은 그 순서대로 터미널에 명령을 넣어 진행하는 흐름이었다.

GWS 설치는 환경에 따라 다르다. npm이 준비되어 있으면 npm 방식으로 설치할 수 있고, macOS에서는 Homebrew를 사용할 수 있다. 강사 환경에서는 Homebrew로 설치한 뒤 `gws --version`으로 설치 완료를 확인했다.

| 준비물 | 역할 | 확인 방법 | 비고 |
|---|---|---|---|
| Visual Studio Code | 실습 폴더 열기 | 공유된 실습 자료 폴더를 연다 | 강의 화면은 VS Code 기준 |
| Node.js | npm 기반 설치의 전제 | `node --version` | 설치 방식에 따라 필요 |
| npm | 패키지 설치 | `npm --version` | GWS 설치 옵션 중 하나 |
| Homebrew | macOS 설치 도구 | `brew --version` | 강사 실습 환경에서 사용 |
| GWS CLI | Workspace 명령 실행 | `gws --version` | 핵심 도구 |
| Google Cloud CLI | Google 계정·프로젝트 인증 | `gcloud --version` | OAuth 설정 흐름에 필요 |

| 명령·동작 | 목적 | 기대 결과 |
|---|---|---|
| 환경 점검 스크립트 실행 | Node, npm, GWS 상태 확인 | 설치된 항목과 누락된 항목이 보인다 |
| GWS 설치 | Google Workspace CLI 설치 | `gws` 명령이 터미널에서 실행된다 |
| `gws --version` | 설치 확인 | 현재 버전이 출력된다 |
| `gws services` | 지원 서비스 확인 | Gmail, Calendar, Drive, Docs, Chat 등 목록이 보인다 |
| Google Cloud CLI 설치 | GWS 인증 준비 | `gcloud` 기반 로그인과 프로젝트 설정이 가능해진다 |

## 3. OAuth 인증은 실습의 가장 큰 분기점이다

GWS를 설치해도 바로 Google Workspace 데이터를 읽을 수 있는 것은 아니다. 캘린더, 지메일, 드라이브 같은 개인 데이터에 접근하려면 Google Cloud 쪽 인증 설정이 필요하다. 강의에서는 `gws auth setup` 흐름을 중심으로 계정 로그인, 프로젝트 선택, OAuth 클라이언트 생성, 권한 범위 선택을 진행했다.

실습 중 강사 환경에서는 새 프로젝트 생성이 프로젝트 한도 문제로 실패했고, 기존 프로젝트를 선택해서 진행했다. 이 장면은 중요하다. 새 프로젝트 생성이 막히더라도, 권한이 있는 기존 프로젝트를 선택해 이어갈 수 있다.

| 단계 | 어디서 하는가 | 해야 할 일 | 성공 신호 |
|---:|---|---|---|
| 1 | 터미널 | `gws auth setup` 실행 | Google Cloud CLI 확인 단계로 진입 |
| 2 | 브라우저 | Google 계정 로그인 | 계정 접근 허용 화면 표시 |
| 3 | 터미널 또는 콘솔 | Google Cloud 프로젝트 선택 또는 생성 | 프로젝트가 선택됨 |
| 4 | Google Cloud Console | Workspace API 활성화 | 필요한 API 목록이 켜짐 |
| 5 | Google Cloud Console | OAuth 동의 화면 구성 | 앱 정보 저장 완료 |
| 6 | Google Cloud Console | OAuth 클라이언트 ID 생성 | 클라이언트 ID와 보안 비밀번호 표시 |
| 7 | 터미널 | 클라이언트 ID와 시크릿 입력 | 인증 URL 생성 |
| 8 | 브라우저 | 권한 범위 승인 | `you may now close this window` 표시 |
| 9 | 터미널 | 인증 결과 확인 | success 출력 |

| 선택지 | 언제 쓰나 | 강의에서의 권장 방향 |
|---|---|---|
| 기존 Google Cloud CLI 활용 | 이미 `gcloud`가 설치되어 있을 때 | 바로 `gws auth setup` 진행 |
| Google Cloud Console 수동 설정 | CLI가 없거나 프로젝트 설정을 직접 해야 할 때 | 콘솔에서 OAuth 동의 화면과 클라이언트 생성 |
| 환경 변수 주입 | CI/CD나 서버 환경일 때 | 개인 실습보다 자동화 환경에 적합 |
| 추천 권한 범위 | 처음 읽기 중심으로 테스트할 때 | 먼저 Recommended로 시작 |
| Full access | 쓰기·삭제까지 모두 자동화할 때 | 필요성을 확인한 뒤 선택 |

## 4. OAuth 동의 화면과 클라이언트 ID 만들기

OAuth 설정에서 참가자들이 가장 헷갈릴 수 있는 지점은 동의 화면과 클라이언트 생성이다. 강의에서는 Google Cloud Console에서 OAuth 동의 화면을 열고, 앱 정보를 저장한 뒤, 사용자 인증 정보에서 OAuth 클라이언트 ID를 만들었다.

애플리케이션 유형은 데스크톱 앱으로 설정했다. 생성이 끝나면 클라이언트 ID와 보안 비밀번호가 나오고, 이 값을 터미널의 `gws auth setup` 흐름에 붙여 넣는다.

| 설정 항목 | 값 또는 방향 | 이유 |
|---|---|---|
| 앱 유형 | 데스크톱 앱 | 로컬 CLI에서 사용자 인증을 진행하기 때문 |
| 클라이언트 ID | 생성된 값을 그대로 복사 | GWS가 Google OAuth 클라이언트를 식별하는 값 |
| 클라이언트 시크릿 | 생성된 보안 비밀번호 복사 | OAuth 인증 요청을 완료하는 데 필요 |
| 앱 상태 | 테스트 중이어도 실습 가능 | 확인되지 않은 앱 경고가 뜰 수 있음 |
| 권한 범위 | 처음에는 추천 읽기 권한 | 불필요한 쓰기·삭제 권한을 피하기 위해 |

| 화면에 보이는 문제 | 의미 | 대응 |
|---|---|---|
| Google Cloud CLI가 설치되지 않았다는 메시지 | `gcloud`가 로컬에 없음 | Google Cloud CLI를 먼저 설치 |
| 프로젝트 생성 실패 | 프로젝트 수 한도 또는 ID 중복 가능성 | 기존 프로젝트 선택 |
| 확인되지 않은 앱 경고 | 앱이 Google 검토를 받지 않은 테스트 상태 | 고급 설정에서 계속 진행 |
| 권한 범위 선택이 많음 | 서비스별 접근 권한이 나뉘어 있음 | 처음에는 Recommended 권한 선택 |
| success가 안 보임 | 브라우저 승인이나 터미널 입력이 완료되지 않음 | 인증 URL 승인과 터미널 로그 재확인 |

## 5. GWS 명령 구조는 서비스명, 액션, 플래그다

GWS 명령은 크게 세 부분으로 이해하면 된다. 첫 번째는 서비스명, 두 번째는 액션, 세 번째는 옵션 플래그다. 예를 들어 캘린더 일정을 보려면 Calendar 서비스와 agenda 또는 event list 계열 액션을 조합한다.

강의에서는 설치와 인증을 마친 뒤 캘린더 조회를 예시로 들었다. 직접 명령으로 일정이 조회되면, 에이전트를 붙이기 전의 기반은 완성된 것이다.

| 구성 요소 | 예시 | 역할 |
|---|---|---|
| CLI 이름 | `gws` | Google Workspace CLI 호출 |
| 서비스명 | `calendar`, `drive`, `gmail`, `docs`, `chat` | 어떤 Workspace 서비스를 다룰지 지정 |
| 액션 | `agenda`, `events list`, `files get`, `about` | 서비스 안에서 수행할 작업 |
| 플래그 | `--limit 2` 같은 옵션 | 출력 개수, 기간, 포맷 등 세부 조건 |

| 확인 작업 | 예시 | 확인할 수 있는 것 |
|---|---|---|
| 버전 확인 | `gws --version` | 설치 정상 여부 |
| 서비스 목록 | `gws services` | 사용 가능한 Workspace 서비스 |
| 캘린더 조회 | `gws calendar agenda` | OAuth 인증과 Calendar API 접근 |
| 스크립트 실행 | 실습 자료의 실행 스크립트 | 드라이브, 지메일, 오늘 일정, 받은 편지 등 복합 조회 |

## 6. AI 에이전트 스킬로 연결하기

단독 CLI가 동작하면 다음 단계는 에이전트 연결이다. 강의에서는 Claude Code를 예로 들었다. Claude Code에 GWS CLI를 통해 스킬을 생성해 달라고 요청하고, 설치 범위를 프로젝트 범위로 지정했다.

스킬이 생성되면 바로 끝이 아니다. 강사는 Claude Code를 종료하고 다시 시작해야 스킬을 인식한다고 설명했다. 재시작 후에는 "오늘 일정 불러줘" 같은 자연어 요청을 입력했고, 에이전트가 백그라운드에서 GWS 캘린더 명령을 실행해 일정을 가져오는 흐름을 확인했다.

| 단계 | 에이전트에서 하는 일 | 목적 | 확인 기준 |
|---:|---|---|---|
| 1 | GWS CLI 스킬 생성을 요청 | CLI 사용법을 에이전트 도구로 만들기 | 계획 또는 설치 범위 질문이 나온다 |
| 2 | 프로젝트 범위 설치 선택 | 현재 실습 폴더에 필요한 스킬만 적용 | 스킬 파일이 프로젝트 안에 생성된다 |
| 3 | 생성 계획 승인 | 에이전트가 파일을 만들게 허용 | GWS 관련 스킬 목록이 보인다 |
| 4 | 에이전트 재시작 | 새 스킬을 로드 | 재시작 후 스킬이 인식된다 |
| 5 | 자연어 요청 실행 | CLI 명령을 직접 외우지 않고 사용 | 오늘 일정 조회 결과가 나온다 |

| 자연어 요청 | 에이전트가 해야 할 해석 | 예상되는 GWS 작업 |
|---|---|---|
| 오늘 일정 불러줘 | 캘린더에서 오늘 일정 조회 | Calendar agenda 조회 |
| 이번 주 일정 요약해줘 | 기간 조건을 잡아 캘린더 읽기 | Calendar events list |
| 내일 오전 10시에 일정 추가해줘 | 쓰기 권한이 있는지 확인 후 일정 생성 | Calendar event create |
| 받은 편지 요약해줘 | Gmail 접근 권한으로 메일 조회 | Gmail message list/read |
| 드라이브 파일 찾아줘 | Drive에서 파일 검색 | Drive files list/search |

## 7. 실습에서 사용한 도구 정리

이번 세션은 특정 하나의 도구만 보는 강의가 아니라, 로컬 CLI, Google Cloud Console, OAuth, AI 에이전트를 이어 붙이는 실습이었다. 그래서 어디에서 무엇을 하는지 분리해서 보는 것이 중요하다.

| 도구 | 쓰인 위치 | 맡은 역할 | 주의할 점 |
|---|---|---|---|
| VS Code | 실습 파일 열기 | 공유된 실습 자료와 스크립트 확인 | 화면 확대와 폴더 위치를 맞춘다 |
| 터미널 | 설치·검증·명령 실행 | GWS와 Google Cloud CLI 실행 | PATH 문제와 명령 오타를 확인한다 |
| GWS CLI | Workspace 데이터 접근 | Calendar, Drive, Gmail 등 API를 명령으로 감싼다 | OAuth 인증 전에는 데이터 조회가 안 된다 |
| Google Cloud CLI | 계정과 프로젝트 인증 | `gws auth setup` 흐름에서 사용 | 설치되어 있지 않으면 먼저 설치한다 |
| Google Cloud Console | OAuth 설정 | 프로젝트, API, 동의 화면, 클라이언트 생성 | 새 프로젝트 한도와 앱 경고가 나올 수 있다 |
| Claude Code | 에이전트 연동 | GWS 스킬 생성과 자연어 요청 실행 | 스킬 생성 뒤 재시작이 필요하다 |
| 실습 스크립트 | 빠른 진행 | 반복 명령을 묶어서 실행 | 내부에서 어떤 GWS 명령을 부르는지 확인한다 |

## 8. 맥 기준 실습과 윈도우 사용자의 차이

강의 자료는 주로 맥 기준으로 준비되어 있었다. 강사는 윈도우도 큰 흐름은 비슷하다고 봤지만, 윈도우 전용 준비는 충분하지 않았다고 말했다. 그래서 윈도우 사용자는 별도로 공유된 링크를 참고하라고 안내했다.

핵심 흐름 자체는 운영체제와 무관하다. GWS 설치, Google Cloud CLI 설치, OAuth 인증, GWS 명령 테스트, 에이전트 스킬 연결이라는 순서는 같다. 다만 설치 명령, 터미널 환경, PATH 반영 방식은 달라질 수 있다.

| 구분 | 맥 | 윈도우 |
|---|---|---|
| 설치 도구 | Homebrew 사용 가능 | 별도 설치 링크와 Windows용 안내 필요 |
| 터미널 | macOS 터미널 또는 VS Code 터미널 | PowerShell 또는 VS Code 터미널 |
| PATH 반영 | brew 설치 후 바로 잡히는 경우가 많음 | 설치 후 새 터미널을 열어야 할 수 있음 |
| 강의 준비도 | 강사 화면 기준 | 추가 참고 자료 안내 |
| 공통 순서 | GWS 설치 후 OAuth 인증 | 동일 |

## 자주 막히는 곳

| 막히는 지점 | 증상 | 원인 | 해결 방향 |
|---|---|---|---|
| 화면이 작아 따라가기 어렵다 | 터미널과 콘솔 글씨가 안 보인다 | 화면 공유 배율 문제 | 발표자에게 확대 요청, 실습 자료를 로컬에서 직접 확인 |
| GWS 명령이 없다 | `gws`가 인식되지 않는다 | 설치 누락 또는 PATH 미반영 | 설치 로그 확인, 새 터미널 열기, `gws --version` 재실행 |
| Google Cloud CLI가 없다고 나온다 | `gws auth setup` 중단 | `gcloud` 미설치 | Google Cloud CLI 설치 후 다시 실행 |
| 프로젝트 생성이 실패한다 | 새 프로젝트가 만들어지지 않는다 | 프로젝트 수 한도 또는 ID 중복 | 기존 프로젝트 선택, 프로젝트 ID를 고유하게 지정 |
| OAuth 화면이 어렵다 | 어떤 메뉴를 눌러야 할지 모른다 | 동의 화면과 사용자 인증 정보 메뉴가 분리됨 | OAuth 동의 화면 저장 후 사용자 인증 정보에서 클라이언트 생성 |
| 확인되지 않은 앱 경고가 나온다 | Google이 앱을 확인하지 않았다는 경고 | 테스트 앱 상태 | 실습에서는 고급 설정으로 계속 진행, 운영 배포는 앱 검토 필요 |
| 권한 범위가 너무 많다 | 어떤 scope를 골라야 할지 모른다 | 서비스별 권한이 세분화됨 | 처음에는 Recommended 읽기 권한으로 시작 |
| CLI 단독 조회가 안 된다 | 캘린더나 메일 결과가 안 나온다 | API 비활성화 또는 권한 부족 | Workspace API 활성화와 OAuth 범위 재확인 |
| 에이전트가 스킬을 못 본다 | 자연어 요청이 GWS로 연결되지 않는다 | 생성 후 재시작을 안 함 | Claude Code를 종료 후 재시작 |
| 쓰기 작업이 실패한다 | 일정 추가 같은 작업이 거절된다 | 읽기 권한만 부여됨 | 필요한 쓰기 scope를 추가하고 재인증 |

## 검증 체크리스트

| 확인 항목 | 통과 기준 |
|---|---|
| GWS 설치 | `gws --version` 출력 |
| 지원 서비스 | `gws services`에서 Gmail, Calendar, Drive 등 확인 |
| Google Cloud CLI | `gcloud` 기반 로그인 가능 |
| OAuth 클라이언트 | 데스크톱 앱 클라이언트 ID와 시크릿 확보 |
| 인증 완료 | 브라우저 승인 후 터미널에 success 표시 |
| 캘린더 조회 | GWS 명령으로 일정 출력 |
| 실습 스크립트 | 드라이브, 지메일, 일정 정보가 한 번에 조회 |
| 에이전트 스킬 | 프로젝트 범위에 GWS 스킬 생성 |
| 자연어 실행 | "오늘 일정 불러줘" 요청에 캘린더 정보 반환 |

## 부록 — 세션에서 남긴 실무 감각

| 장면 | 실무적으로 해석하면 |
|---|---|
| CLI 설치가 막히자 에이전트에게 설치를 맡김 | 에이전트는 코딩뿐 아니라 로컬 환경 문제 해결에도 쓸 수 있다 |
| 프로젝트 생성 한도에 걸림 | 실습에서는 이상적인 흐름보다 실제 계정 상태가 더 중요하다 |
| 권한 범위를 추천값으로 시작함 | 자동화는 처음부터 모든 권한을 주기보다 읽기 중심으로 검증하는 편이 낫다 |
| 스킬 생성 후 재시작함 | 에이전트 도구 체계는 파일 생성과 로딩 시점이 다를 수 있다 |
| 윈도우 자료를 별도로 안내함 | 운영체제별 설치 차이는 본 실습의 주요 리스크다 |
